class Solution {
    public ListNode middleNode(ListNode head) {
        ListNode slow = head;
        while(head!=null&& head.next!= null){
            head= head.next.next;
            slow= slow.next;
        }
        return slow;
    }
}
